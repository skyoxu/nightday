# Task 11: 示例 taskdoc

本文件是 taskdoc 的示例，用于展示 `scripts/sc/analyze.py` 如何把 `taskdoc/<id>.md` 注入到 `task_context.<id>.json` 里。

建议内容：

- 当前任务的关键术语/上下文
- 需要特别注意的边界条件
- 与 ADR/CH/Overlay 的对应关系（只写路径/编号，不复制正文）

